@if (State == EState.Busy)
{
    <div class="alert alert-info" role="alert">
        @ChildContent
    </div>
}

@code {

    [Parameter]
    public RenderFragment? ChildContent { get; set; } 
    
    [Parameter] public EState State { get; set; }

}